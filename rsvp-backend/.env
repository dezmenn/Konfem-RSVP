# Demo Environment Configuration
NODE_ENV=development
PORT=5000

# Skip database and Redis setup for demo
SKIP_DB_SETUP=true
SKIP_REDIS_SETUP=true

# Frontend URLs for CORS
FRONTEND_URLS=http://localhost:3000,http://localhost:19006

# Log level
LOG_LEVEL=info